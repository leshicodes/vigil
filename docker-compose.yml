services:
  vigil:
    build: .
    container_name: vigil
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - VIGIL_CAMERA=mock
      - VIGIL_PORT=8080
    restart: unless-stopped
