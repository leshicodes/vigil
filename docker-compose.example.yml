services:
  vigil:
    image: leshicodes/vigil:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      # Uncomment to develop hooks without rebuilding:
      # - ./hooks:/home/vigil/hooks
    env_file:
      - .env
    restart: unless-stopped